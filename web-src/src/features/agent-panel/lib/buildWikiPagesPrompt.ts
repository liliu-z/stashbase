/** This is exactly what the user sees and exactly what the Agent receives.
 * The durable Wiki Pages behavior lives in Agent Instructions, so the action
 * never hides a second product prompt behind concise transcript copy. */
export const BUILD_WIKI_PAGES_PROMPT = 'Build or update Wiki Pages from these Sources.';
